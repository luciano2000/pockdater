name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 9.0.100
    - name: Restore dependencies
      run: dotnet restore pockdater.csproj
    - name: Build
      run: dotnet build pockdater.csproj --no-restore
    - name: Test
      run: dotnet test pockdater.csproj --no-build --verbosity normal
